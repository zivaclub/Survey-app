# Ensure native binaries for target platforms are always installed
# This prevents "module not found" errors in CI/CD (Linux) when building on Mac
supportedArchitectures=cpu:x64,arm64;os:linux,darwin
